// (temporary diagnostic script — no longer used; safe to delete)
// It previously sent a non-captured ₪0.01 test order to /api/checkout/create-order
// to check whether PAYPAL_* env vars are set in the production deployment.